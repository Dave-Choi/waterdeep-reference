<div class="container-fluid">
	<div class="row">
		<div class="col-sm-4">
			{{collapsing-list
				title = "Lords"
				list = model
				itemRoute = "lord"
				itemDisplayProperty = "name"
			}}
		</div>

		<div class="col-sm-8">
			{{#bootstrap-affixed}}
				{{outlet}}
			{{/bootstrap-affixed}}
		</div>
	</div>
</div>
